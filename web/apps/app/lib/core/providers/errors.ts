/**
 * Re-exported from @oriveo/core so the web and desktop builds share one implementation:
 * the ProviderError types plus toProviderError / networkError / asProviderErrorObject.
 */
export * from '@oriveo/core/providers/errors';
