// Shell: Relay endpoint URL construction lives in @oriveo/core/providers/relay-endpoints, shared by web and the desktop main process.
export {
  normalizeRelayBaseURL,
  buildRelayEndpointURL,
  type RelayEndpointTransport,
} from '@oriveo/core/providers/relay-endpoints';
