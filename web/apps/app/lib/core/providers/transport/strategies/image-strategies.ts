/**
 * Image strategies live in @oriveo/core so that every client imports the same implementation.
 * This module re-exports them so importers here keep their existing path.
 */
export * from '@oriveo/core/providers/transport/strategies/image-strategies';
