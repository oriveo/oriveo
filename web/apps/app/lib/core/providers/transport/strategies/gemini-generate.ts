/**
 * The Gemini generate strategy lives in @oriveo/core, so the web and desktop builds share one
 * implementation. This module re-exports it under the path web importers use.
 */
export * from '@oriveo/core/providers/transport/strategies/gemini-generate';
