/**
 * Re-export of the protocol core from @oriveo/core, so web and desktop share one implementation
 * and existing web importers keep working unchanged.
 */
export * from '@oriveo/core/providers/transport/strategies/openai-responses';
