/**
 * The transport kind definitions live in @oriveo/core so every consumer shares one copy.
 * This module re-exports them so existing web importers keep their current import paths.
 */
export * from '@oriveo/core/providers/transport/transport-kind';
