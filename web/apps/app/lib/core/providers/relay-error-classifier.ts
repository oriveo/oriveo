/**
 * Re-export of the classifier from @oriveo/core, so every client shares one implementation and
 * local importers can keep using this path.
 */
export * from '@oriveo/core/providers/relay-error-classifier';
