# macOS

The macOS client is not part of this release. This directory is a placeholder so the
repository layout matches the other clients.

In the meantime the web client runs well as a desktop app in any browser, and the iOS
build runs on Apple silicon Macs.
